version: '3'
services:
  task-management-db:
    image: postgres:latest
    container_name: task-management-db
    environment:
      POSTGRES_PASSWORD: mysecretpassword
    ports:
      - "5432:5432"
    volumes:
      - task-management-volume:/var/lib/postgresql/data

volumes:
  task-management-volumne: